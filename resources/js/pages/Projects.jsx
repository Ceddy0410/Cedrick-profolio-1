import { Head, Link } from '@inertiajs/react';
import { ArrowUpRight } from 'lucide-react';
import SiteLayout from '../components/SiteLayout';
import { clientSites, projects } from '../data/portfolio';

export default function Projects() {
    return <SiteLayout><Head title="Selected Work" /><section className="page-hero section-pad"><div><p className="eyebrow"><span />Selected work</p><h1>Built, tested, and improved with purpose.</h1><p>Personal projects, a team capstone, and agency client contributions—each described with a clear account of my role.</p></div></section><section className="projects-page section-pad"><div className="project-stack">{projects.map((project, index) => <Link className={`project-card project-card--${project.accent}`} href={`/projects/${project.slug}`} key={project.slug}><div className="project-index">0{index + 1}</div><div className="project-copy"><p>{project.type}</p><h2>{project.title}</h2><span>{project.summary}</span><strong>My contribution</strong><span>{project.contribution}</span><div className="tag-row">{project.tools.map((tool) => <span key={tool}>{tool}</span>)}</div></div><div className="project-status">View case study<ArrowUpRight size={20} /></div></Link>)}</div></section><section className="client-work section-pad"><div className="section-heading"><p className="kicker">Live client websites</p><h2>Contributions delivered through GodesQ.</h2><p className="section-note">These were agency and team engagements. Each case study will describe my contribution without claiming sole ownership.</p></div><div className="client-links">{clientSites.map(([name, url]) => <a href={url} target="_blank" rel="noreferrer" key={url}><span>{name}</span><ArrowUpRight /></a>)}</div></section></SiteLayout>;
}
